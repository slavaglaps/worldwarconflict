{
  "name": "wwc-build-tools",
  "private": true,
  "description": "Build tooling (esbuild) for the WWC client — bundles/minifies the game + sim for production.",
  "scripts": {
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
